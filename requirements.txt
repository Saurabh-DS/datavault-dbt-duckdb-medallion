# DataVault — Python dependencies
# Install with: pip install -r requirements.txt
#
# Versions are pinned to ensure reproducibility across environments.
# Python 3.11 required.

# ── dbt Core + DuckDB adapter ────────────────────────────────────────────────
dbt-core==1.8.7
dbt-duckdb==1.8.4
duckdb==1.0.0

# ── Synthetic data generation ─────────────────────────────────────────────────
faker==26.0.0
pandas==2.2.2
numpy==1.26.4

# ── Apache Airflow ─────────────────────────────────────────────────────────────
# Install Airflow with the postgres and slack extras for orchestration support.
# If you only want to run the dbt pipeline without Airflow, comment these out.
apache-airflow==2.9.1
apache-airflow-providers-slack==8.7.1

# ── Development utilities ──────────────────────────────────────────────────────
sqlfluff==3.1.0          # SQL linting (optional but recommended)
pre-commit==3.7.1        # Git hooks for linting on commit
